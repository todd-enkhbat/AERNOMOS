"""Phase M: lightweight real CPU execution (no GPUs).

Real plan-step execution on the existing ARQ worker. Durations and byte
counts are measured for every run and persisted as OBSERVED — never stubbed.
"""
