import { ExamplesLibrary } from "@/components/examples/ExamplesLibrary";

export const metadata = {
  title: "Example plans · Nomos Orbital",
  description:
    "Curated public example mission plans with explicit real, calculated, estimated, simulated, and unavailable disclosures."
};

export default function ExamplesPage() {
  return <ExamplesLibrary />;
}
